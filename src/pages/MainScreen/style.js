import styled from "styled-components";
import COLORS from "../../common/colors";

export const Container = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;

  background: ${COLORS.MODAL_GRAY};
  height: 100vh;
`;
